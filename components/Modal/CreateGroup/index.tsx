import classNames from "classnames/bind";

import styles from "./CreateGroup.module.scss";

const cn = classNames.bind(styles);

export default function CreateGroupModal() {
  return <div>그룹 생성 모달</div>;
}
