import classNames from "classnames/bind";

import styles from "./UpcomingSchedules.module.scss";

const cn = classNames.bind(styles);

export default function UpcomingSchedules() {
  return <div>index</div>;
}
